import Constants from "expo-constants";

export default {
  header: {
    alignItems: "center",
    flexDirection: "row",
    justifyContent: "center",
    paddingTop: Constants.statusBarHeight + 20,
    marginBottom: 10,
  },
};
